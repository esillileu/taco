# Intent

## Problem

Teams repeatedly spend tokens and time rediscovering the same project context during agent-driven coding.

## Objective

Deliver deterministic, task-level context packs from documentation so agents can execute work with minimal drift.

## Success Criteria

- A task can be started from a single MCP call.
- Context assembly is deterministic for identical repository state.
- Completion routing identifies exactly where to write outcomes.
