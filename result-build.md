`PROJ-ENTRYPOINT-BUILD` is already present at [.context/project/entrypoint-build.md](/home/esillileu/taco/uroboros/.context/project/entrypoint-build.md) and matches the content you posted, so I made no changes.

Repo state note: that file is currently untracked (`git status` shows `?? .context/project/entrypoint-build.md`). If you want, I can stage it and create a commit next.